
using UnityEngine;

public class CheckDistances : MonoBehaviour
{
    private Enemy enemy;

    //private void Update()
    //{
    //    enemy.CheckChasingDistance(distanceFromPlayer() < enemy.chasingDistance);
    //    enemy.SetAggroStatus(distanceFromPlayer() < enemy.aggroDistanse);
    //    enemy.CheckShootingDistance(distanceFromPlayer() < enemy.shootingDistance);
    //    Debug.Log(distanceFromPlayer());
    //}

    //protected float distanceFromPlayer()
    //{
    //    return Vector2.Distance(enemy.target.transform.position, enemy.transform.position);
    //}
}
